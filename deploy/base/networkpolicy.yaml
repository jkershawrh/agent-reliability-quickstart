apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: agent-reliability-default-deny}
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels: {network.openshift.io/policy-group: ingress}
    - from:
        - podSelector: {}

